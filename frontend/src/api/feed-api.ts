import { BaseApi } from "./base-api";

export class feedAPI extends BaseApi{
    public static async getUserFeeds(){
        try{
            const response = await this.client.get<>
        }
    }
}